# HecateOS Environment Variables
# Copy this file to .env and set secure values
# NEVER commit .env to git!

# Default user password (use a secure password or generate one)
HECATE_PASSWORD=CHANGE_ME_TO_SECURE_PASSWORD

# Root password (optional, defaults to HECATE_PASSWORD)
ROOT_PASSWORD=CHANGE_ME_TO_SECURE_PASSWORD

# ISO build settings
ISO_BUILD_DIR=/tmp/hecateos-build
ISO_OUTPUT_DIR=./output